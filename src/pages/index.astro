---
import { Image } from "astro:assets";
import type { ImageMetadata } from "astro";
import BaseLayout from "../layouts/BaseLayout.astro";
import SectionReveal from "../components/SectionReveal.astro";
import CategoryCard from "../components/CategoryCard.astro";
import WhatsAppCTA from "../components/WhatsAppCTA.astro";
import ChatDemo from "../components/ChatDemo.tsx";
import PromoShowcase from "../components/PromoShowcase.astro";
import TrustSection from "../components/TrustSection.astro";
import { siteConfig } from "../lib/site-config";
import { categories as categoryMeta } from "../lib/categories";

import heroImage from "../assets/products/muebles/camilla-3-niveles.jpg";
// nails-2.jpg y makeup-3.jpg: fotos de estudio limpias (fondo blanco), no las
// fotos de producto individuales tomadas en tienda -- esas tienen estantes y
// reflejos de fondo que no funcionan en un tratamiento editorial a pantalla
// completa (ver feedback de evaluación de diseño). nails-2.jpg ya se usaba
// así en nosotros.astro; se repite aquí por consistencia.
import nailsImage from "../assets/products/unas/nails-2.jpg";
import hairImage from "../assets/products/capilares/keratina-ambroisie-250ml-rene-chardon.jpg";
import makeupImage from "../assets/products/maquillaje/makeup-3.jpg";
import mueblesImage from "../assets/products/muebles/silla-barberia-st2200.jpg";
import sillonDestacado from "../assets/products/muebles/sillon-peluqueria-1.jpg";
import camillaDestacada from "../assets/products/muebles/camilla-3-niveles.jpg";
import mesaDestacada from "../assets/products/muebles/mesa-silla-concha-manicura.jpg";

// Fotografía por categoría para el mosaico -- lib/categories.ts trae el
// texto/orden/tamaño (misma fuente que el header), esto solo mapea
// slug -> asset local. Un mapa de datos, no un if/else por categoria.
const categoryImages: Record<(typeof categoryMeta)[number]["slug"], ImageMetadata> = {
  muebles: mueblesImage,
  unas: nailsImage,
  capilares: hairImage,
  maquillaje: makeupImage,
};

// Mosaico: la categoria "large" (por tamaño de catálogo, no por nombre) va
// como banner ancho arriba; el resto en fila pareja abajo -- se filtra por
// el campo size de cada categoria, asi que una categoria nueva marcada
// "medium" simplemente se suma a la fila sin tocar este archivo.
const categoryBanner = categoryMeta.filter((c) => c.size === "large");
const categoryGrid = categoryMeta.filter((c) => c.size !== "large");

const mueblesDestacados = [
  { name: "Sillón de peluquería ST-2202", price: "$345", image: sillonDestacado, slug: "sillon-de-peluqueria-st-2202" },
  { name: "Camilla multiuso 3 niveles", price: "$208", image: camillaDestacada, slug: "camilla-multiuso-3-niveles" },
  { name: "Set mesa + silla concha", price: "$350", image: mesaDestacada, slug: "set-mesa-silla-concha-para-manicura" },
];
---

<BaseLayout title={siteConfig.name} description={siteConfig.description} path="/">
  <section class="relative overflow-hidden bg-primary-900">
    <div
      class="pointer-events-none absolute inset-0 opacity-50"
      style="background-image: radial-gradient(circle at 15% 15%, rgba(227,28,77,0.4), transparent 55%), radial-gradient(circle at 85% 85%, rgba(227,28,77,0.25), transparent 50%);"
      aria-hidden="true"
    >
    </div>

    <div class="relative mx-auto grid max-w-6xl items-center gap-16 px-6 py-20 sm:py-28 lg:grid-cols-[1.05fr_1fr] lg:gap-10 lg:py-32">
      <div>
        <p class="text-sm font-semibold uppercase tracking-[0.3em] text-blush-100">
          {siteConfig.tagline}
        </p>
        <h1 class="text-display-xl mt-5 text-white">El proveedor completo para tu salón de belleza</h1>
        <p class="mt-6 max-w-lg text-lg text-white/80">
          Muebles, insumos de uñas, tratamientos capilares y maquillaje profesional — todo en
          Portoviejo, con precio y disponibilidad al instante por WhatsApp.
        </p>

        <ul class="mt-6 flex flex-wrap gap-2" aria-label="Categorías disponibles">
          {
            categoryMeta.map((category) => (
              <li>
                <a
                  href={category.href}
                  class="inline-flex items-center rounded-full border border-white/25 px-4 py-1.5 text-sm text-white/90 transition-colors hover:border-white/60 hover:bg-white/10"
                >
                  {category.shortLabel}
                </a>
              </li>
            ))
          }
        </ul>

        <div class="mt-8 flex flex-wrap gap-4">
          <WhatsAppCTA label="Chatea por WhatsApp" pulse source="home" pageType="home" ctaLocation="hero" />
          <a
            href="/productos"
            class="inline-flex items-center gap-2 rounded-full border-2 border-white px-6 py-3 font-semibold text-white transition-transform duration-200 hover:scale-105 hover:bg-white hover:text-primary-900"
          >
            Ver catálogo completo
          </a>
        </div>
      </div>

      <div class="relative mx-auto aspect-[5/6] w-full max-w-md lg:max-w-none">
        <div class="editorial-overlay group absolute inset-x-0 top-0 aspect-[4/5] w-[76%] overflow-hidden rounded-[1.75rem] shadow-2xl">
          <Image
            src={heroImage}
            alt="Camilla profesional Alva Importaciones"
            class="media-zoom h-full w-full bg-white object-cover"
            width={640}
            height={800}
            densities={[1, 1.5, 2]}
            loading="eager"
            fetchpriority="high"
          />
          <div class="editorial-overlay pointer-events-none absolute inset-0"></div>
        </div>

        <div class="group absolute bottom-0 right-0 aspect-square w-[48%] overflow-hidden rounded-2xl border-4 border-cream shadow-xl">
          <Image
            src={nailsImage}
            alt="Insumos profesionales para uñas Alva Importaciones"
            class="media-zoom h-full w-full bg-white object-cover"
            width={420}
            height={420}
            densities={[1, 1.5, 2]}
            loading="eager"
          />
        </div>

        <div class="group absolute -left-2 bottom-10 hidden aspect-square w-[36%] overflow-hidden rounded-2xl border-4 border-cream shadow-lg sm:block lg:-left-6">
          <Image
            src={makeupImage}
            alt="Maquillaje profesional Alva Importaciones"
            class="media-zoom h-full w-full bg-white object-cover"
            width={320}
            height={320}
            densities={[1, 1.5, 2]}
            loading="eager"
          />
        </div>
      </div>
    </div>
  </section>

  <SectionReveal as="section" class="section-y px-6">
    <div class="mx-auto max-w-6xl">
      <p class="text-center text-sm font-semibold uppercase tracking-[0.3em] text-primary-700">
        Compra por categoría
      </p>
      <h2 class="text-display-lg mt-2 text-center text-primary-900">Todo tu salón, en un solo lugar</h2>
      <p class="mx-auto mt-3 max-w-2xl text-center text-ink-muted">
        Precio y stock exactos siempre por WhatsApp.
      </p>
      <div class="mt-12 flex flex-col gap-4">
        {
          categoryBanner.map((category) => (
            <CategoryCard
              title={category.label}
              description={category.description}
              image={categoryImages[category.slug]}
              href={category.href}
              aspectClass="aspect-[4/3] sm:aspect-[21/9]"
            />
          ))
        }
        <div class="grid gap-4 sm:grid-cols-3">
          {
            categoryGrid.map((category) => (
              <CategoryCard
                title={category.label}
                description={category.description}
                image={categoryImages[category.slug]}
                href={category.href}
              />
            ))
          }
        </div>
      </div>
    </div>
  </SectionReveal>

  <SectionReveal as="section" class="px-6 py-20">
    <div class="mx-auto max-w-6xl">
      <p class="text-center text-sm font-semibold uppercase tracking-[0.3em] text-primary-700">
        Lo más nuevo
      </p>
      <h2 class="mt-2 text-center font-display text-3xl text-primary-900">
        Muebles para tu salón
      </h2>
      <p class="mx-auto mt-3 max-w-2xl text-center text-ink-muted">
        Camillas, sillones y estaciones de trabajo listos para tu negocio.
      </p>
      <div class="mt-12 grid gap-6 sm:grid-cols-3">
        {
          mueblesDestacados.map((m) => (
            <div class="glass-card group overflow-hidden rounded-2xl shadow-md transition-transform duration-300 hover:-translate-y-1 hover:shadow-xl">
              <a href={`/productos/muebles/${m.slug}/`} class="block">
                <div class="aspect-square overflow-hidden bg-white">
                  <Image
                    src={m.image}
                    alt={m.name}
                    width={500}
                    height={500}
                    class="h-full w-full object-contain transition-transform duration-500 group-hover:scale-105"
                  />
                </div>
              </a>
              <div class="flex flex-col gap-2 p-5">
                <a href={`/productos/muebles/${m.slug}/`} class="font-display text-lg text-primary-800 hover:text-primary-600">
                  {m.name}
                </a>
                <p class="text-lg font-semibold text-ink">{m.price}</p>
                <WhatsAppCTA
                  label="Consultar"
                  class="mt-1 w-fit !px-4 !py-2 text-sm"
                  message={`Hola, quisiera consultar sobre: ${m.name}`}
                  source="home"
                  pageType="home"
                  ctaLocation="product_highlight"
                  productId={m.slug}
                  productName={m.name}
                  category="muebles"
                />
              </div>
            </div>
          ))
        }
      </div>
      <div class="mt-10 text-center">
        <a
          href="/productos/muebles"
          class="inline-flex items-center gap-2 rounded-full bg-primary-800 px-6 py-3 font-semibold text-white transition-transform hover:scale-105 hover:bg-primary-700"
        >
          Ver todos los muebles →
        </a>
      </div>
    </div>
  </SectionReveal>

  <SectionReveal as="section" class="bg-blush-50 px-6 py-20">
    <PromoShowcase />
  </SectionReveal>

  <SectionReveal as="section" id="asistente-ia" class="bg-blush-50 px-6 py-20">
    <div class="mx-auto grid max-w-6xl items-center gap-12 lg:grid-cols-2">
      <div>
        <p class="text-sm font-semibold uppercase tracking-[0.3em] text-primary-700">
          Asistente por WhatsApp
        </p>
        <h2 class="mt-4 font-display text-3xl text-primary-900">
          Respuestas al instante, con precio y stock reales
        </h2>
        <p class="mt-6 text-ink-muted">
          Creamos un asistente de inteligencia artificial que responde tus consultas de
          WhatsApp en segundos: precios actualizados, disponibilidad real y sugerencias si
          un producto se agotó. Cuando hace falta, una asesora toma la conversación.
        </p>
        <WhatsAppCTA label="Pruébalo por WhatsApp" class="mt-8" source="home" pageType="home" ctaLocation="chat_demo" />
      </div>
      <ChatDemo client:visible />
    </div>
  </SectionReveal>

  <SectionReveal as="section" class="px-6 py-20">
    <div class="mx-auto grid max-w-6xl items-center gap-12 lg:grid-cols-2">
      <div class="order-2 lg:order-1">
        <div class="glass-card rounded-3xl p-8">
          <p class="font-display text-2xl text-primary-800">Curso Profesional de Uñas Acrílicas</p>
          <p class="mt-4 text-ink-muted">
            6 meses de formación profesional. $60 al mes (o 2 pagos de $30), con matrícula
            de $10 para separar tu cupo.
          </p>
          <a
            href="/cursos"
            class="mt-6 inline-block font-semibold text-primary-700 hover:underline"
          >
            Conoce el curso →
          </a>
        </div>
      </div>
      <div class="order-1 lg:order-2">
        <h2 class="font-display text-3xl text-primary-900">¿Quieres vivir de las uñas?</h2>
        <p class="mt-6 text-ink-muted">
          Además de vender los insumos, formamos a la próxima generación de nail artists en
          Portoviejo con nuestro curso profesional.
        </p>
      </div>
    </div>
  </SectionReveal>

  <SectionReveal as="section" class="bg-blush-50 px-6 py-20">
    <TrustSection />
  </SectionReveal>

  <SectionReveal as="section" class="px-6 pb-24">
    <div class="mx-auto max-w-3xl rounded-3xl bg-primary-800 px-8 py-16 text-center text-white">
      <h2 class="font-display text-3xl">¿Lista para tu próximo pedido?</h2>
      <p class="mt-4 text-blush-100/90">
        Escríbenos por WhatsApp, visita uno de nuestros locales o revisa el catálogo completo —
        te ayudamos al instante.
      </p>
      <div class="mt-8 flex flex-wrap justify-center gap-4">
        <WhatsAppCTA label="Chatea por WhatsApp" variant="outline" class="!border-white !text-white hover:!bg-white hover:!text-primary-800" source="home" pageType="home" ctaLocation="bottom" />
        <a
          href="/locales"
          class="inline-flex items-center gap-2 rounded-full border-2 border-white px-6 py-3 font-semibold text-white transition-transform hover:scale-105 hover:bg-white hover:text-primary-800"
        >
          Visitar un local
        </a>
      </div>
    </div>
  </SectionReveal>
</BaseLayout>
